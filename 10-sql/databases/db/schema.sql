-- Write code here or enter directly in PostgreSQL shell --
-- Drop the existing 'sample_db' if it exists
DROP DATABASE IF EXISTS sample_db;

-- Create a new database named 'books_db'
CREATE DATABASE books_db;

-- Connect to the new 'books_db'
\connect books_db

